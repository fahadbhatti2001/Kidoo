import 'package:flutter/material.dart';
class Kidoo extends StatelessWidget {
  const Kidoo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
